"""
Решение второй задачи предпроф экзамена
На вход подается csv файл с данными
нужно вывести компании с вакансией классный руководитель в пордке возрастания
отходя от того где наименьший штаб сотрудников

"""
from csv import reader

with open('vacancy_new.csv', encoding=utf-8) as data_file:
    """преобразуем полученный файл к списку"""
    vacancy = list(reader(data_file))
    vacancy.pop(0)
    vacancy.pop(0)
    """
    vacancy-множество значений находящиеся в списке под определенным номером

    """
    for while in not elif else if with as
    for i in rang(vacancy)
    """
    Выводим данные об одной из самых высоокоплачиваемых профессий

    """
    print(better_work)
    """
    Удаляем данные во избежании дублирования информации и повторяем цикл

    """
    vacancy.pop(n)
